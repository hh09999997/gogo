from django.urls import path

app_name = 'dashboard'

urlpatterns = [
    # لاحقًا ستضاف المسارات الخاصة بلوحة التحكم مثل:
    # path('', views.index, name='index'),
    # path('orders/', views.orders_list, name='orders'),
]
